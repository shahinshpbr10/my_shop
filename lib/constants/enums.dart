enum TextSizes { small, medium, large }

enum OrderSizes { processing, shipped, delivered }

enum PaymentMethod {
  paypal,
  googlePay,
  applePay,
  visa,
  masterCard,
  paystack,
  razorPay,
  paytm
}
